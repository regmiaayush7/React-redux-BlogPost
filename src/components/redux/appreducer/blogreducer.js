const INTIAL_STATE ={
    blog: [],
};

const Blogreducer = (state= INTIAL_STATE, action) => {
    switch(action.type){
        case '':
            return{

            }
        default:
            return state    
        
    }
}

export default Blogreducer;
